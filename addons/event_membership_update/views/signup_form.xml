<odoo>
    <template id="auth_signup_fields_inherit" inherit_id="auth_signup.fields" name="Extend Signup Fields">
        <xpath expr="//div[@class='form-group position-relative']" position="after">
            
            <!-- Region Dropdown -->
            <div class="form-group">
                <label for="region_id">Region</label>
                <select id="region_id" name="region_id" class="form-control">
                    <option value="">Select Region</option>
                </select>
            </div>

            <!-- Province Dropdown -->
            <div class="form-group">
                <label for="province_id">Province</label>
                <select id="province_id" name="province_id" class="form-control">
                    <option value="">Select Province</option>
                </select>
            </div>

            <!-- City Dropdown -->
            <div class="form-group">
                <label for="city_id">City</label>
                <select id="city_id" name="city_id" class="form-control">
                    <option value="">Select City</option>
                </select>
            </div>

            <!-- Barangay Dropdown -->
            <div class="form-group">
                <label for="barangay_id">Barangay</label>
                <select id="barangay_id" name="barangay_id" class="form-control">
                    <option value="">Select Barangay</option>
                </select>
            </div>

        </xpath>
    </template>
</odoo>
