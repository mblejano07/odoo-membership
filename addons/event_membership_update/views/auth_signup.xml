<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="auth_signup_inherit_fields" inherit_id="auth_signup.fields">
        <xpath expr="//input[@name='confirm_password']" position="after">

            <!-- Region Field -->
            <div class="form-group">
                <label for="region_id">Region</label>
                <select id="region_id" name="region_id" class="form-control form-control-sm" required="required">
                    <option value="">Select Region</option>
                </select>
            </div>

            <!-- Province Field -->
            <div class="form-group">
                <label for="province_id">Province</label>
                <select id="province_id" name="province_id" class="form-control form-control-sm" required="required">
                    <option value="">Select Province</option>
                </select>
            </div>

            <!-- City Field -->
            <div class="form-group">
                <label for="city_id">City</label>
                <select id="city_id" name="city_id" class="form-control form-control-sm" required="required">
                    <option value="">Select City</option>
                </select>
            </div>

            <!-- Barangay Field -->
            <div class="form-group">
                <label for="barangay_id">Barangay</label>
                <select id="barangay_id" name="barangay_id" class="form-control form-control-sm" required="required">
                    <option value="">Select Barangay</option>
                </select>
            </div>

        </xpath>
    </template>
</odoo>
